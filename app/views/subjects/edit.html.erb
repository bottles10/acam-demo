<h1 class="mb-8 text-4xl text-center font-bold"> Edit Subject</h1>
<div class="mb-4">
  <%= render "form", subject: @subject %>
</div>

<%= link_to "&larr; Cancel".html_safe, subject_path(@subject), class: "btn-cancel" %>