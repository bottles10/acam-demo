<div class="mb-8 flex items-center justify-between md:justify-end">
  <h1 class="md:hidden text-4xl font-bold text-gray-800">Subjects</h1>

  <%= link_to "Assign Subject", new_subject_path, 
                class: "btn-primary hover:bg-blue-500 
                        hover:scale-105 transition duration-300" %>
</div>

<h1 class="hidden md:block md:mb-8 text-4xl text-center font-bold text-gray-800">Subjects</h1>

<div class="flex justify-center">
  <%= render 'subject', subjects: @subjects %>
</div>
